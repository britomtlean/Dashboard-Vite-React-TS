export type Login = {
  cpf: string;
  senha: string;
};

export type Casdastro = {
  nome: string;
  cpf: string;
  email: string;
  senha: string;
};

export type UpdateUser = {
  nome: string,
  email: string
}
